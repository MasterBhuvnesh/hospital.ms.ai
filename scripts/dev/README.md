# scripts/dev

Local environment lifecycle: bootstrap a fresh clone, seed demo data, reset to a known state.

The seed refuses to run when `APP_ENV=production`. Seeded credentials are documented in [`docs/developer.md`](../../docs/developer.md) section 2 and exist only for `development`.
