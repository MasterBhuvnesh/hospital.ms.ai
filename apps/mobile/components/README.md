# apps/mobile/components

React Native components for the patient application.

`packages/ui` is a web and Electron renderer package and is **not** imported here: React Native does not share the DOM. The design tokens are shared; the components are not.
