# apps/mobile/app

Expo Router. File-based routing, matching the mental model of the web application.

The flagship screen is the live queue (`/queue/[tokenId]`), which is also the deep-link target for the near-turn and your-turn notifications.
