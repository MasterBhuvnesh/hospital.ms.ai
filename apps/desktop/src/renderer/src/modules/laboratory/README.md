# desktop / laboratory

**P3.** Collection, results and verification.

View assigned orders, record sample collection, enter results, upload documents, and run the verification workflow: `entered` to `verified` to `released`.

**Results reach the patient only on release**, never at entry.
