# apps/web/components

Components used only by the web application.

Anything the desktop renderer also needs belongs in `@hms/ui` instead. If you are about to copy a component into `apps/desktop`, move it to `packages/ui` and import it in both.
